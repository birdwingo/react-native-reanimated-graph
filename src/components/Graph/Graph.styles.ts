import { StyleSheet } from 'react-native';

export default StyleSheet.create( {
  globalContainer: {
    flex: 1,
    width: '100%',
  },
  container: {
    flex: 1,
    width: '100%',
    flexDirection: 'row',
  },
} );
