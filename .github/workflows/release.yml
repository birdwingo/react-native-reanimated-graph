name: Create Github Release
on:
  push:
    branch: test
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16.x'
          registry-url: 'https://registry.npmjs.org'
      - run: npm ci
      - name: Set up git user for release
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.AUTH_TOKEN }}@github.com/birdwingo/react-native-reanimated-graph
      - run: npm run release
      - name: Push changes
        run: git push --follow-tags origin main
      - run: npm run build
      - name: Get version from package-lock.json
        id: get_version
        run: echo "::set-output name=version::$(node -p "require('./package-lock.json').version")"
      - name: Get changelog
        run: |
          CHANGELOG=$(tail -n +$(grep -n "## \[v${{ steps.get_version.outputs.version }}\]" CHANGELOG.md | cut -d ":" -f 1) CHANGELOG.md)
          echo "::set-output name=changelog::${CHANGELOG}"
      - name: Create Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: "v${{ steps.get_version.outputs.version }}"
          release_name: "v${{ steps.get_version.outputs.version }}"
          body: ${{ steps.get_changelog.outputs.changelog }}